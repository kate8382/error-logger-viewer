NODE_ENV=development
PORT=3000
API_PROXY_TARGET=http://localhost:3000

# Optional: move the LowDB JSON file outside backend/db.json, for example
# when running with a Docker volume.
DB_FILE=backend/db.json
